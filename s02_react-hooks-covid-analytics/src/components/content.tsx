import { FC } from "react";

const Content: FC = () => {
    return (
        <section className="layout-main">
            <div className="cmp__cards">
            </div>
        </section>
    );
}

export default Content;